# Taller Grupal 2
## Efectos colaterales
**Semana**: 6

**Objetivos**:
- Realizar operaciones con estructuras de datos (tuplas, listas).
- Comprender y aplicar los tipos `Option`, `Try` y `Either` para el manejo seguro de errores.

---

### Descripción:
Formará grupos, por afinidad, de 4 integrantes. Cada grupo debe designar a uno de sus integrantes como su responsable que será el encargado de: organizar el grupo garantizando el trabajo de los demás integrantes, será el único que podrá plantear preguntas al tutor y sobretodo debe garantizar que todos comprenden el problema y la solución. Si el grupo lo considera necesario podrán cambiar de líder, las veces que lo consideren oportuno.

---

### Ejercicio 1: Raíz cuadrada segura (Option)

Implementa una función `safeSqrt` que reciba un número `Double` y devuelva su raíz cuadrada como un `Option[Double]`. La función debe seguir estas reglas:

1. Si el número es negativo, devuelve `None` (no existe raíz cuadrada real de números negativos).
2. Si el número es mayor o igual a cero, devuelve el resultado como `Some[Double]`.

```scala
// Implementa tu función aquí
def safeSqrt(number: Double): Option[Double] = {
  
}

// Pruebas
println(safeSqrt(25))    // Some(5.0)
println(safeSqrt(0))     // Some(0.0)
println(safeSqrt(2))     // Some(1.4142135623730951)
println(safeSqrt(-9))    // None
println(safeSqrt(-1))    // None
```

---

### Ejercicio 2: Validar edad de usuario (Either)

Implementa una función `validarEdad` que reciba una edad (`Int`) y devuelva un `Either[String, Int]`. La función debe validar que la edad sea válida para registrar a un usuario en una plataforma.

**Reglas:**
1. Si la edad es negativa, devuelve `Left` con el mensaje: `"Error: La edad no puede ser negativa"`
2. Si la edad es menor a 18, devuelve `Left` con el mensaje: `"Error: Debe ser mayor de 18 años"`
3. Si la edad es mayor a 120, devuelve `Left` con el mensaje: `"Error: Edad no válida"`
4. Si la edad está en el rango válido (18 a 120), devuelve `Right` con la edad

```scala
// Implementa tu función aquí
def validarEdad(edad: Int): Either[String, Int] = {
  
}

// Pruebas
println(validarEdad(25))     // Right(25)
println(validarEdad(18))     // Right(18)
println(validarEdad(120))    // Right(120)
println(validarEdad(15))     // Left(Error: Debe ser mayor de 18 años)
println(validarEdad(-5))     // Left(Error: La edad no puede ser negativa)
println(validarEdad(150))    // Left(Error: Edad no válida)
```

---

### Ejercicio 3: Conversión de texto a fecha (Try)

Implementa una función `parseDate` que reciba un `String` con formato de fecha y devuelva un `Try[LocalDate]`. La función debe intentar convertir el texto a una fecha válida.

**Reglas:**
1. El formato esperado es `"dd/MM/yyyy"` (ejemplo: `"25/12/2024"`)
2. Si el texto tiene el formato correcto y representa una fecha válida, devuelve `Success` con el `LocalDate`
3. Si el texto no puede ser convertido (formato incorrecto o fecha inválida), devuelve `Failure` con la excepción correspondiente

**Ejemplo de conversión de texto a fecha:**
```scala
import java.time.LocalDate
import java.time.format.DateTimeFormatter

val fechaTexto: String = "15/08/2023"
val formato: DateTimeFormatter = DateTimeFormatter.ofPattern("dd/MM/yyyy")
val fecha: LocalDate = LocalDate.parse(fechaTexto, formato)
println(fecha)  // 2023-08-15
```

**Tu tarea:** Completar el siguiente código.
```scala
import scala.util.{Try, Success, Failure}
import java.time.LocalDate
import java.time.format.DateTimeFormatter

// Implementa tu función aquí
def parseDate(dateString: String): ... = {
  
}

val fechasTexto: List[String] = List(
  "01/01/2024",
  "15/08/2023",
  "2024/02/31",
  "hola",
  "25/12/2024"
)

// Completa el código para obtener una List[Try[LocalDate]]
val fechasConvertidas = ???

// Resultado esperado:
// List(
//   Success(2024-01-01),
//   Success(2023-08-15),
//   Failure(DateTimeParseException),
//   Failure(DateTimeParseException),
//   Success(2024-12-25)
// )
```



---

### Calificación:
Para la calificación, se debe presentar el trabajo realizado a su tutor. Es necesario recalcar que la presentación se hace una única vez, no existe la posibilidad de presentaciones adicionales con correcciones.

Tal como se le explicó anteriormente, los talleres se calificarán de la siguiente manera:
- **10 puntos** si presenta en el horario de prácticas y experimentación.
- **7 puntos** si presenta en el horario de tutoría.

La hora máxima de presentación será las **16h20**.